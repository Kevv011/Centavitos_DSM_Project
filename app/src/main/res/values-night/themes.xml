<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DSM_Centavitos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- For now, we follow the same palette to avoid default purple -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/brand_green</item>
        <item name="android:windowBackground">@color/canvas_dark</item>
        <item name="colorSurface">@color/canvas_dark</item>
        <item name="colorOnSurface">@color/on_primary</item>
    </style>
</resources>
